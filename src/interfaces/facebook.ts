export interface FacebookAdAccount {
  accessToken?: string | null;
  id: string | null;
  currency?: string | null;
  name: string | null;
}

export interface FacebookCampaigns {
  id: string;
  name: string;
}
