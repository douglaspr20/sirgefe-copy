export interface PageView {
  id: string;
  url: string;
  campaign: string;
  ad_set: string;
  ad: string;
  conversion_value: string;
  created: string;
  referer: string;
  source: string;
}
